import React from "react";
import { createRoot } from "react-dom/client";
import App from "../../PnP_FNB_Survey_2026.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
